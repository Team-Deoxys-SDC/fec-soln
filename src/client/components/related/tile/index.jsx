import React, { useContext } from 'react';
import { AppContext } from '../../../contexts';

export default function RelatedTile () {
  return (
    <div style={{ height: '300px', width: '225px', border: '1px solid black' }}>
      <img />
    </div>
  );
}
